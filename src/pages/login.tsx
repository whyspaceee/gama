import Head from "next/head";
import { signIn } from "next-auth/react";
import { useRouter } from "next/router";
import { FcGoogle } from "react-icons/fc";
import { FaDiscord } from "react-icons/fa";
import Image from "next/image";
import Link from "next/link";

export default function Login() {
  const { callbackUrl } = useRouter().query;

  return (
    <>
      <Head>
        <title>Merchant Login</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className="flex min-h-screen flex-col items-center justify-between bg-main px-8 py-20">
        <div>
          <h1 className=" text-4xl font-bold text-white tracking-wider">GAS! Merchant</h1>
          <p className=" text-m flex flex-col items-center pb-25 font-bold tracking-wider text-white">
            Become a GAMA Merchant now
          </p>
        </div>
        <Image
          className="logo-neumorphic mb-15 outline-fill rounded-3xl"
          src="/gama-logo2.png"
          width={200}
          height={200}
          alt="logo"
        />
        <div className=" w-full">
          <div className="inline-flex w-full items-center justify-center">
            <hr className="my-8 h-px w-full border bg-gray-200 " />
            <span className="absolute left-1/2 -translate-x-1/2 bg-main px-6 font-bold text-white">
              Log in with
            </span>
          </div>
          <div className=" flex w-full flex-row gap-4 ">
            <button
              className="  h-12 w-full rounded-xl border bg-white text-lg  font-bold text-white "
              onClick={() => {
                signIn("google", {
                  callbackUrl: callbackUrl ? callbackUrl.toString() : "/",
                });
              }}
            >
              <FcGoogle className=" mx-auto h-6 w-6" />
            </button>
            <button
              className="  h-12 w-full rounded-xl bg-white text-lg font-bold text-white"
              onClick={() => {
                signIn("discord", {
                  callbackUrl: callbackUrl ? callbackUrl.toString() : "/",
                });
              }}
            >
              <FaDiscord className=" mx-auto h-6 w-6 fill-black" />
            </button>
          </div>
        </div>
        <div className="flex flex-col items-center justify-center mt-">
        </div>
      
      </main>
    </>
  );
}
